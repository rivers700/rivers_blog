# 环境变量配置示例
# 复制此文件为 .env.local 并修改配置

# ========================================
# 安全配置（必须修改！）
# ========================================

# 管理员密码 - 必须修改为安全的密码！
# 建议使用 16 位以上的随机字符串
ADMIN_PASSWORD=your_secure_password_here

# JWT 密钥 - 用于签名认证 Token
# 建议使用 32 位以上的随机字符串
# 可以使用命令生成: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_change_in_production

# 可选：使用密码哈希代替明文密码（更安全）
# 使用 bcrypt 生成: node -e "console.log(require('bcryptjs').hashSync('your_password', 12))"
# ADMIN_PASSWORD_HASH=$2a$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ========================================
# 网站配置
# ========================================

# 网站地址 - 部署后修改为你的域名（用于 sitemap、RSS、Open Graph）
SITE_URL=https://yourdomain.com

# ========================================
# 可选配置
# ========================================

# Node 环境
NODE_ENV=production
